# T-Bank (Tinkoff) Configuration
TBANK_TERMINAL_KEY=
TBANK_PASSWORD=

# Server Configuration
PORT=3001
NODE_ENV=development

# SMTP (emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="MiniAppExpert <no-reply@miniapp.expert>"

# Magic link signing
MAGIC_SECRET=

# Production values (when ready):
# NODE_ENV=production

